## TAG: 96 — PBD Non Connected - Left Voicemail

No conversation took place. The rep left a voicemail.

This call does not advance BANT or any deal metric. Carry all
accumulated deal state forward unchanged. The only thing to evaluate
is the quality of the voicemail itself as a touchpoint.

---

## WHAT TO EVALUATE

**1. Voicemail quality**
- Did the rep mention the partner relationship?
- Was the message concise and specific — or generic and forgettable?
- Did the rep leave a clear reason to call back?
- Did they leave contact details?

---

## WIN RATE SCORE

Measures voicemail execution quality only.

80–100 — Partner mentioned + specific reason to call back + contact details left
50–79 — Partner mentioned but generic message, or no clear call-to-action
0–49 — No partner mention, generic message, or no contact details

---

## OUTPUT FORMAT

All fields carry forward from prior context unchanged. No deal progress
occurred in this call.

{
  "win_rate_score": integer (0–100),
  // Voicemail execution quality only.

  "forecast_flag": "GREEN" | "AMBER" | "RED",
  // Cumulative. Unchanged.

  "partner_leverage_score": integer (1–4),
  // Cumulative. Update only if voicemail referenced the partner well.

  "lead_temperature": "Hot" | "Warm" | "Cold",
  // Cumulative. Unchanged.

  "bant": {
    "budget":    { "status": "Confirmed|Partial|Missing", "evidence": "string or null" },
    "authority": { "status": "Confirmed|Partial|Missing", "evidence": "string or null" },
    "need":      { "status": "Confirmed|Partial|Missing", "evidence": "string or null" },
    "timing":    { "status": "Confirmed|Partial|Missing", "evidence": "string or null" }
  },
  // Carry forward unchanged.

  "script_compliance": {
    "opener":         "PASS" | "FLAG" | "FAIL",
    "industry_pivot": "PASS" | "FLAG" | "FAIL",
    "close":          "PASS" | "FLAG" | "FAIL",
    "two_slot_close": boolean
  },
  // Cumulative. Unchanged.

  "objections": "string or null",
  // Carry forward unchanged.

  "buying_signals": "string or null",
  // Carry forward unchanged.

  "blockers": "string or null",
  // Carry forward unchanged.

  "rep_strengths": "string or null",
  // Full process analysis — not just this call.

  "improvement_items": ["string"],
  // Max 3. Flag only if voicemail had no partner mention or was generic.

  "tag_validation": "VALID" | "WRONG_TAG",
  // VALID: voicemail left, no live conversation
  // WRONG_TAG: actual conversation occurred

  "deal_context": "string",
  // 1. BEFORE: full deal state — BANT, prior attempts, what is known
  // 2. THIS CALL: voicemail left — quality and content summary
  // 3. NOW: deal state unchanged — awaiting callback or next attempt

  "deal_status": "Iniciando" | "Cualificando" | "Demo pendiente" | "Demo agendada" | "Estancado",
  // Carry forward unchanged.

  "biggest_gap": "string",
  // Carry forward from prior context.

  "next_call_objective": "string",
  // Follow up if no callback received within agreed timeframe.

  "next_action_rep": "string",

  "tl_note": "string or null",
  // Escalate only if pattern of repeated voicemails with no contact
  // suggests the prospect is not reachable or the number is wrong.

  "top_coaching_flag": "string or null",

  "hard_question": "string or null"
}
